# Core vision
opencv-python>=4.8.0
mediapipe>=0.10.0

# EAR calculation
scipy>=1.11.0
numpy>=1.24.0

# Audio (ordered by preference — install all for best compatibility)
sounddevice>=0.4.6   # Fallback beep generator (no WAV file needed)
playsound==1.2.2     # Simple fallback (Linux may need extra setup)
